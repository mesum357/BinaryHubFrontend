<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Binary Hub | Empowering the Next Generation</title>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:wght@300;400;600&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
    
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --primary-orange: #ee702b;
            --primary-blue: #005696;
            --white: #ffffff;
            --dark-bg: #050b16;
            --deep-bg: #03070d;
            --light-gray: #1a1f2e;
            --text-main: #e0e6ed;
            --text-dim: #a0aec0;
            --glass: rgba(255, 255, 255, 0.05);
            --glass-dark: rgba(0, 0, 0, 0.4);
            --header-font: 'Space Grotesk', sans-serif;
            --body-font: 'Inter', sans-serif;
            --display-font: 'Bebas Neue', sans-serif;
            --transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        /* Global Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            cursor: none; /* Hide default for custom cursor */
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: var(--body-font);
            background: linear-gradient(135deg, var(--deep-bg), var(--dark-bg));
            color: var(--text-main);
            overflow-x: hidden;
            line-height: 1.6;
            min-height: 100vh;
        }

        /* Grain Texture */
        .noise-overlay {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            pointer-events: none;
            z-index: 9999;
            opacity: 0.03;
            background: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3仿真 %3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
        }

        /* Scroll Progress Bar */
        .scroll-progress {
            position: fixed;
            top: 0; left: 0; width: 0%; height: 4px;
            background: var(--primary-orange);
            z-index: 10001;
        }

        /* Loading Screen */
        #loader {
            position: fixed;
            inset: 0;
            background: var(--dark-bg);
            z-index: 10002;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            transition: opacity 0.8s ease;
        }

        .loader-logo {
            width: 200px;
            opacity: 0;
            transform: scale(0.8);
            animation: logo-pop 1s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }

        .loader-logo img {
            width: 100%;
            height: auto;
        }

        @keyframes logo-pop {
            to { opacity: 1; transform: scale(1); }
        }

        /* Custom Cursor */
        #cursor-glow {
            position: fixed;
            width: 300px;
            height: 300px;
            background: radial-gradient(circle, rgba(238,112,43,0.15) 0%, rgba(0,86,150,0.05) 50%, transparent 70%);
            border-radius: 50%;
            pointer-events: none;
            z-index: 9998;
            transform: translate(-50%, -50%);
            transition: width 0.3s, height 0.3s, background 0.3s;
        }

        #cursor-dot {
            position: fixed;
            width: 8px;
            height: 8px;
            background: var(--primary-orange);
            border-radius: 50%;
            pointer-events: none;
            z-index: 10005;
            transform: translate(-50%, -50%);
            transition: transform 0.1s ease-out, width 0.3s, height 0.3s;
        }

        /* Typography Helpers */
        h1, h2, h3 { font-family: var(--header-font); }
        .gradient-text {
            background: linear-gradient(90deg, var(--primary-orange), var(--primary-blue));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .section-padding { padding: 100px 10%; }

        /* Animation Reveal */
        .reveal {
            opacity: 0;
            transform: translateY(40px);
            transition: 1s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0; width: 100%;
            padding: 20px 10%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            transition: var(--transition);
        }

        nav.scrolled {
            background: rgba(5, 11, 22, 0.85);
            backdrop-filter: blur(20px);
            padding: 15px 10%;
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }

        .logo {
            display: flex;
            align-items: center;
            height: 50px;
            transition: var(--transition);
        }

        .logo img {
            height: 100%;
            width: auto;
            object-fit: contain;
        }

        nav.scrolled .logo {
            height: 40px;
        }

        .nav-links {
            display: flex;
            gap: 30px;
            list-style: none;
        }

        .nav-links li { position: relative; }

        .nav-links a {
            text-decoration: none;
            color: white; /* Default state */
            font-weight: 600;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            padding-bottom: 5px;
            transition: color 0.3s;
        }

        nav.scrolled .nav-links a {
            color: white; /* Keep white even when scrolled on dark theme */
            opacity: 0.8;
        }

        nav.scrolled .nav-links a:hover {
            opacity: 1;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -2px; left: 0;
            width: 0%; height: 2px;
            background: var(--primary-orange);
            transition: 0.3s ease;
        }

        .nav-links a:hover::after, .nav-links a.active-link::after {
            width: 100%;
        }

        .mobile-toggle {
            display: none;
            font-size: 1.5rem;
            cursor: pointer;
            color: white; /* Default state */
            z-index: 1001;
            transition: color 0.3s;
        }

        nav.scrolled .mobile-toggle {
            color: white; /* Keep white even when scrolled */
        }

        /* Dropdown */
        .dropdown { position: relative; }
        .dropdown-menu {
            position: absolute;
            top: 100%; left: 0;
            background: rgba(255,255,255,0.95);
            backdrop-filter: blur(10px);
            min-width: 200px;
            padding: 15px;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            opacity: 0; visibility: hidden;
            transform: translateY(20px);
            transition: var(--transition);
        }

        .dropdown:hover .dropdown-menu {
            opacity: 1; visibility: visible;
            transform: translateY(0);
        }

        .dropdown-menu a {
            display: block;
            padding: 8px 0;
            color: #555 !important; /* Ensure dropdown items stay visible on white bg */
            font-size: 0.85rem;
        }

        .dropdown-menu a:hover { color: var(--primary-orange); padding-left: 5px; }

        /* Hero Slider */
        .hero {
            height: 100vh;
            position: relative;
            overflow: hidden;
        }

        .slide {
            position: absolute;
            inset: 0;
            opacity: 0;
            transition: opacity 1s ease-in-out;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
        }

        .slide.active { opacity: 1; }

        .slide-bg {
            position: absolute;
            inset: 0;
            background-size: cover;
            background-position: center;
            transform: scale(1.1);
            transition: transform 6s ease;
            z-index: -1;
        }

        .slide.active .slide-bg { transform: scale(1); }

        .slide-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(to right, rgba(0,86,150,0.7), rgba(238,112,43,0.5));
            z-index: 0;
        }

        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 900px;
            padding: 0 20px;
        }

        .hero-eyebrow { font-size: 1.2rem; text-transform: uppercase; letter-spacing: 4px; margin-bottom: 20px; opacity: 0; }
        .hero-title { font-family: var(--display-font); font-size: 8vw; line-height: 0.9; margin-bottom: 20px; }
        .hero-subtext { font-size: 1.2rem; margin-bottom: 30px; opacity: 0; }

        .active .hero-eyebrow { animation: fadeUp 0.8s forwards 0.3s; }
        .active .hero-subtext { animation: fadeUp 0.8s forwards 0.7s; }

        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .btn {
            padding: 15px 40px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            display: inline-block;
            transition: 0.3s;
            border: none;
            font-family: var(--header-font);
        }

        .btn-primary {
            background: var(--primary-orange);
            color: white;
            box-shadow: 0 10px 20px rgba(238,112,43,0.3);
        }

        .btn-primary:hover {
            transform: scale(1.05);
            background: var(--primary-blue);
        }

        /* Founder Section */
        .founder-section {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            gap: 50px;
            position: relative;
        }

        .founder-card {
            flex: 1;
            min-width: 300px;
            position: relative;
        }

        .founder-img-wrapper {
            width: 400px; height: 400px;
            border-radius: 50%;
            margin: 0 auto;
            position: relative;
            padding: 10px;
            border: 2px dashed #eee;
        }

        .founder-img-wrapper::before {
            content: '';
            position: absolute;
            inset: -5px;
            border-radius: 50%;
            border: 5px solid transparent;
            border-top-color: var(--primary-orange);
            border-bottom-color: var(--primary-blue);
            animation: spin 10s linear infinite;
        }

        @keyframes spin { to { transform: rotate(360deg); } }

        .founder-img {
            width: 100%; height: 100%;
            border-radius: 50%;
            object-fit: cover;
        }

        .founder-info {
            flex: 1;
            min-width: 300px;
        }

        .quote-bg {
            position: absolute;
            font-size: 15rem;
            color: rgba(238,112,43,0.1);
            top: -50px; left: -20px;
            z-index: -1;
            font-family: serif;
        }

        .pull-quote {
            background: linear-gradient(135deg, var(--primary-blue), var(--primary-orange));
            padding: 30px;
            color: white;
            border-radius: 20px;
            margin-top: 30px;
            font-style: italic;
            font-size: 1.2rem;
        }

        /* Impact Section (Orbital Layout) */
        .impact-section {
            background: radial-gradient(circle at center, #0f172a, #050b16);
            text-align: center;
            overflow: hidden;
            position: relative;
        }

        .impact-container {
            height: 800px;
            position: relative;
            margin-top: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .impact-center {
            width: 250px; height: 250px;
            background: radial-gradient(circle, var(--primary-blue), var(--primary-orange));
            border-radius: 50%;
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            box-shadow: 0 0 70px rgba(0,86,150,0.4);
            z-index: 10;
            font-size: 1.2rem;
            transition: var(--transition);
        }

        .impact-center:hover { transform: translate(-50%, -50%) scale(1.05); }

        .orbit-item {
            width: 180px; height: 180px;
            background: var(--light-gray);
            border: 2px solid rgba(255,255,255,0.1);
            border-radius: 50%;
            position: absolute;
            top: 50%; left: 50%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            transition: 0.5s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 15px 30px rgba(0,0,0,0.3);
            z-index: 5;
            overflow: hidden;
            color: white;
            text-shadow: 0 2px 10px rgba(0,0,0,0.5);
        }

        .orbit-item img {
            position: absolute;
            inset: 0;
            width: 100%; height: 100%;
            object-fit: cover;
            z-index: -1;
            transition: transform 0.5s ease;
        }

        .orbit-item::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(to bottom, rgba(0,0,0,0.1), rgba(0,86,150,0.6));
            z-index: -1;
        }

        .orbit-item:hover img {
            transform: scale(1.1);
        }

        .orbit-item i { font-size: 2rem; margin-bottom: 10px; color: white; filter: drop-shadow(0 2px 5px rgba(0,0,0,0.3)); }
        .orbit-item span { font-size: 0.85rem; font-weight: bold; text-transform: uppercase; letter-spacing: 1px; }

        /* Services Grid */
        .services-section {
            background: var(--dark-bg);
            color: white;
        }

        .service-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 30px;
            margin-top: 50px;
        }

        .service-card {
            height: 320px;
            perspective: 1000px;
        }

        .card-inner {
            position: relative;
            width: 100%; height: 100%;
            transition: transform 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            transform-style: preserve-3d;
        }

        .service-card:hover .card-inner { transform: rotateY(180deg); }

        .card-face {
            position: absolute;
            width: 100%; height: 100%;
            backface-visibility: hidden;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 30px;
            border-radius: 20px;
            border: 1px solid rgba(255,255,255,0.1);
            background: #111;
        }

        .card-back {
            background: linear-gradient(135deg, var(--primary-orange), var(--primary-blue));
            transform: rotateY(180deg);
            text-align: center;
        }

        .service-icon {
            font-size: 3rem;
            margin-bottom: 20px;
            color: var(--primary-orange);
        }

        /* Blog Section */
        .blog-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 50px;
        }

        .blog-card {
            background: var(--light-gray);
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 15px 40px rgba(0,0,0,0.2);
            transition: 0.4s;
            border: 1px solid rgba(255,255,255,0.05);
        }

        .blog-card:hover { transform: translateY(-10px); }

        .blog-img {
            width: 100%; height: 200px;
            background: #eee;
            background-size: cover;
            background-position: center;
        }

        .blog-content { padding: 25px; }

        /* Footer */
        footer {
            background: #050b16;
            color: white;
            padding: 80px 10% 30px;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 50px;
            margin-bottom: 50px;
        }

        .footer-col h4 {
            margin-bottom: 25px;
            font-size: 1.2rem;
            color: var(--primary-orange);
        }

        .footer-col ul { list-style: none; }
        .footer-col ul li { margin-bottom: 12px; }
        .footer-col a { color: #aaa; text-decoration: none; transition: 0.3s; }
        .footer-col a:hover { color: white; padding-left: 5px; }

        .social-icons { display: flex; gap: 15px; margin-top: 20px; }
        .social-icons a:hover { background: var(--primary-orange); transform: translateY(-5px); }

        .linkedin-btn {
            background: #0077b5 !important;
            color: white !important;
            width: auto !important;
            padding: 10px 25px !important;
            border-radius: 50px !important;
            display: flex !important;
            align-items: center;
            gap: 10px;
            font-size: 0.9rem;
            font-weight: 600;
            text-decoration: none;
            transition: 0.3s;
            box-shadow: 0 10px 20px rgba(0,119,181,0.2);
        }

        .linkedin-btn:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 15px 30px rgba(0,119,181,0.3);
            background: #005a8a !important;
        }

        /* Scroll to Top */
        #backToTop {
            position: fixed;
            bottom: 30px; right: 30px;
            width: 50px; height: 50px;
            background: var(--primary-orange);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            visibility: hidden;
            transition: 0.3s;
            z-index: 999;
            box-shadow: 0 5px 15px rgba(238,112,43,0.4);
        }

        #backToTop.show { opacity: 1; visibility: visible; }

        /* Responsive */
        @media (max-width: 768px) {
            .hero-title { font-size: 3rem; }
            
            .mobile-toggle { display: block; }
            
            .nav-links {
                position: fixed;
                top: 0; right: -100%;
                width: 70%; height: 100vh;
                background: rgba(5, 11, 22, 0.98);
                backdrop-filter: blur(20px);
                flex-direction: column;
                justify-content: center;
                align-items: center;
                gap: 20px;
                transition: 0.5s ease-in-out;
                display: flex; /* Override display:none */
                box-shadow: -10px 0 30px rgba(0,0,0,0.3);
            }

            .nav-links.active { right: 0; }
            
            .nav-links li { width: 100%; text-align: center; }
            .nav-links a { font-size: 1.2rem; display: block; width: 100%; }

            .dropdown-menu {
                position: static;
                opacity: 1;
                visibility: visible;
                display: none;
                transform: none;
                background: rgba(255,255,255,0.03);
                box-shadow: none;
                margin-top: 10px;
                width: 100%;
            }

            .dropdown.open .dropdown-menu {
                display: block;
            }
            
            .dropdown.open > a i {
                transform: rotate(180deg);
            }

            .impact-container { 
                height: auto; 
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
                gap: 20px;
                padding: 40px 0;
            }

            .impact-center { 
                position: relative; 
                top: 0; left: 0; 
                transform: none; 
                margin: 0 auto 30px;
                width: 150px; height: 150px;
            }

            .orbit-item { 
                position: relative !important; 
                top: 0 !important; left: 0 !important; 
                transform: none !important; 
                width: 100%; 
                height: auto; 
                padding: 30px 15px; 
                border-radius: 20px;
            }

            .orbit-item:hover { transform: translateY(-10px) !important; }
            
            .founder-img-wrapper { width: 280px; height: 280px; }
            .section-padding { padding: 60px 5%; }
        }

        @media (min-width: 769px) and (max-width: 1024px) {
            .impact-container { height: 750px; }
            .impact-center { width: 220px; height: 220px; font-size: 1.1rem; }
            .orbit-item { width: 150px; height: 150px; }
        }
    </style>
</head>
<body>

    <!-- Loading Screen -->
    <div id="loader">
        <div class="loader-logo">
            <img src="/images/logo-binary.png" alt="Binary Hub Logo">
        </div>
    </div>

    <div class="noise-overlay"></div>
    <div class="scroll-progress" id="progressBar"></div>
    <div id="cursor-glow"></div>
    <div id="cursor-dot"></div>

    <!-- Navigation -->
    <nav id="navbar">
        <a href="/" class="logo">
            <img src="/images/logo-binary.png" alt="Binary Hub Logo">
        </a>
        <ul class="nav-links">
            <li><a href="/" class="active-link">Home</a></li>
            <li class="dropdown">
                <a href="/our-mission">About Us <i class="fas fa-chevron-down" style="font-size: 0.6rem;"></i></a>
                <div class="dropdown-menu">
                    <a href="/team">Our Team</a>
                    <a href="/our-mission">Our Mission</a>
                    <a href="/impact">Our Impact</a>
                    <a href="/branches">Our Branches</a>
                </div>
            </li>
            <li><a href="/courses">Our Courses</a></li>
            <li class="dropdown">
                <a href="/events">Gallery <i class="fas fa-chevron-down" style="font-size: 0.6rem;"></i></a>
                <div class="dropdown-menu">
                    <a href="/events">Events</a>
                    <a href="/visits">Visits</a>
                </div>
            </li>
            <li><a href="/services">Services</a></li>
            <li><a href="/internships">Internships</a></li>
        </ul>
        <div class="mobile-toggle" id="mobileToggle"><i class="fas fa-bars"></i></div>
    </nav>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <!-- Slide 1 -->
        <div class="slide active">
            <div class="slide-bg" style="background-image: url('https://images.unsplash.com/photo-1550745165-9bc0b252726f?q=80&w=2070&auto=format&fit=crop');"></div>
            <div class="slide-overlay"></div>
            <div class="hero-content">
                <p class="hero-eyebrow">Welcome to Binary Hub</p>
                <h1 class="hero-title">SHAPING DIGITAL FUTURES</h1>
                <p class="hero-subtext">Empowering youth through technology, creativity, and innovation.</p>
                <a href="/courses" class="btn btn-primary">Explore Programs</a>
            </div>
        </div>
        <!-- Slide 2 -->
        <div class="slide">
            <div class="slide-bg" style="background-image: url('https://images.unsplash.com/photo-1531297484001-80022131f5a1?q=80&w=2020&auto=format&fit=crop');"></div>
            <div class="slide-overlay"></div>
            <div class="hero-content">
                <p class="hero-eyebrow">Learn & Grow</p>
                <h1 class="hero-title">WORLD-CLASS COURSES</h1>
                <p class="hero-subtext">From Web Development to AI — master the skills of tomorrow.</p>
                <a href="#courses" class="btn btn-primary">View Courses</a>
            </div>
        </div>
        <!-- Slide 3 -->
        <div class="slide">
            <div class="slide-bg" style="background-image: url('https://images.unsplash.com/photo-1522202176988-66273c2fd55f?q=80&w=2071&auto=format&fit=crop');"></div>
            <div class="slide-overlay"></div>
            <div class="hero-content">
                <p class="hero-eyebrow">Our Impact</p>
                <h1 class="hero-title">TRANSFORMING LIVES</h1>
                <p class="hero-subtext">Scholarships, vocational training, and community empowerment.</p>
                <a href="/our-mission" class="btn btn-primary">Our Mission</a>
            </div>
        </div>

        <div class="slider-controls" style="position:absolute; bottom:30px; left:50%; transform:translateX(-50%); z-index:10; display:flex; gap:15px;">
            <div class="dot active" onclick="setSlide(0)" style="width:12px; height:12px; background:var(--primary-orange); border-radius:50%;"></div>
            <div class="dot" onclick="setSlide(1)" style="width:12px; height:12px; background:white; border-radius:50%;"></div>
            <div class="dot" onclick="setSlide(2)" style="width:12px; height:12px; background:white; border-radius:50%;"></div>
        </div>
    </section>

    <!-- Founder Section -->
    <section class="section-padding reveal">
        <div class="founder-section">
            <div class="founder-card">
                <div class="founder-img-wrapper">
                    <img src="/images/tehseensir.jpg" alt="Tahseen Abbas" class="founder-img">
                </div>
                <div style="text-align: center; margin-top: 20px;">
                    <h3>Tahseen Abbas</h3>
                    <p style="color: var(--primary-orange);">Founder Binary Hub</p>
                    <div class="social-icons" style="justify-content: center; margin-top: 15px;">
                        <a href="#" class="linkedin-btn"><i class="fab fa-linkedin-in"></i> Follow on LinkedIn</a>
                    </div>
                </div>
            </div>
            <div class="founder-info">
                <span class="quote-bg">“</span>
                <h2 style="font-size: 3rem; margin-bottom: 20px;">A Message From <br><span class="gradient-text">Our Founder</span></h2>
                <p>At Binary Hub, we believe that technology is the ultimate equalizer. Our mission is to bridge the digital divide by providing the youth with high-quality education and professional opportunities.</p>
                <div class="pull-quote" style="background: linear-gradient(135deg, rgba(0,86,150,0.3), rgba(238,112,43,0.3)); border: 1px solid rgba(255,255,255,0.1);">
                    "The future isn't something we enter; it's something we create through the power of code and creativity."
                </div>
                <a href="#" style="display:inline-block; margin-top: 25px; color: var(--primary-blue); font-weight: bold; text-decoration: none;">Read Full Story →</a>
            </div>
        </div>
    </section>

    <!-- Impact Section -->
    <section class="impact-section section-padding">
        <h2 class="reveal" style="font-size: 3.5rem;">Delivering <span class="gradient-text">Impact</span></h2>
        <p class="reveal">Our reach across communities and beyond</p>
        
        <div class="impact-container reveal">
            <svg id="orbit-lines" style="position: absolute; inset: 0; width: 100%; height: 100%; pointer-events: none; z-index: 0;"></svg>
            <div class="impact-center">
                <div style="text-align: center;">
                    <span style="font-size: 2rem; display: block;">OUR</span>
                    IMPACT
                </div>
            </div>

            <!-- Orbital Items with Images -->
            <div class="orbit-item">
                <img src="https://images.unsplash.com/photo-1529390079861-591de354faf5?q=80&w=2070&auto=format&fit=crop" alt="Youth Empowerment">
                <i class="fas fa-star"></i>
                <span>Youth Empowerment</span>
            </div>
            <div class="orbit-item">
                <img src="https://images.unsplash.com/photo-1523050335392-9beffa71386b?q=80&w=2070&auto=format&fit=crop" alt="Scholarships">
                <i class="fas fa-graduation-cap"></i>
                <span>Scholarships</span>
            </div>
            <div class="orbit-item">
                <img src="https://images.unsplash.com/photo-1573497019940-1c28c88b4f3e?q=80&w=1974&auto=format&fit=crop" alt="Women Power">
                <i class="fas fa-venus"></i>
                <span>Women Power</span>
            </div>
            <div class="orbit-item">
                <img src="https://images.unsplash.com/photo-1513258496099-48168024adb0?q=80&w=2070&auto=format&fit=crop" alt="Vocational">
                <i class="fas fa-tools"></i>
                <span>Vocational</span>
            </div>
            <div class="orbit-item">
                <img src="https://images.unsplash.com/photo-1517694712202-14dd9538aa97?q=80&w=2070&auto=format&fit=crop" alt="Tech Ed">
                <i class="fas fa-laptop-code"></i>
                <span>Tech Ed</span>
            </div>
            <div class="orbit-item">
                <img src="https://images.unsplash.com/photo-1488521787991-ed7bbaae773c?q=80&w=2070&auto=format&fit=crop" alt="Outreach">
                <i class="fas fa-hands-helping"></i>
                <span>Outreach</span>
            </div>
        </div>

        <div style="display: flex; justify-content: space-around; margin-top: 100px;" class="reveal">
            <div><h3 style="font-size: 3rem; color: var(--primary-orange);">500+</h3><p>Students Graduated</p></div>
            <div><h3 style="font-size: 3rem; color: var(--primary-blue);">20+</h3><p>Courses Offered</p></div>
            <div><h3 style="font-size: 3rem; color: var(--primary-orange);">5</h3><p>Global Branches</p></div>
        </div>
    </section>

    <!-- Services Section -->
    <section class="services-section section-padding" id="services">
        <div style="text-align: center;">
            <h2 class="reveal" style="font-size: 3.5rem;">What We <span class="gradient-text">Offer</span></h2>
            <p class="reveal">Cutting-edge services tailored for your digital success</p>
            <a href="/services" class="btn btn-primary reveal" style="margin-top: 24px; display: inline-block;">View all services</a>
        </div>

        <div class="service-grid">
            <!-- Card 1 -->
            <div class="service-card reveal">
                <div class="card-inner">
                    <div class="card-face card-front">
                        <div class="service-icon"><i class="fas fa-code"></i></div>
                        <h3>Web Development</h3>
                    </div>
                    <div class="card-face card-back">
                        <p>Scalable, high-performance websites built with the latest frameworks.</p>
                        <a href="/services/web-dev" class="btn btn-primary" style="margin-top: 20px; display: inline-block;">Details</a>
                    </div>
                </div>
            </div>
            <!-- Card 2 -->
            <div class="service-card reveal">
                <div class="card-inner">
                    <div class="card-face card-front">
                        <div class="service-icon" style="color: #00d2ff;"><i class="fas fa-paint-brush"></i></div>
                        <h3>UI/UX Design</h3>
                    </div>
                    <div class="card-face card-back">
                        <p>User-centric designs that prioritize experience and conversion.</p>
                        <a href="/services/uiux" class="btn btn-primary" style="margin-top: 20px; display: inline-block;">Details</a>
                    </div>
                </div>
            </div>
            <!-- Card 3 -->
            <div class="service-card reveal">
                <div class="card-inner">
                    <div class="card-face card-front">
                        <div class="service-icon" style="color: #ff9d00;"><i class="fas fa-robot"></i></div>
                        <h3>AI & ML</h3>
                    </div>
                    <div class="card-face card-back">
                        <p>Harness the power of Artificial Intelligence for your business.</p>
                        <a href="/services/ai" class="btn btn-primary" style="margin-top: 20px; display: inline-block;">Details</a>
                    </div>
                </div>
            </div>
            <!-- Card 4 -->
            <div class="service-card reveal">
                <div class="card-inner">
                    <div class="card-face card-front">
                        <div class="service-icon" style="color: #ff0055;"><i class="fas fa-video"></i></div>
                        <h3>Video Editing</h3>
                    </div>
                    <div class="card-face card-back">
                        <p>Professional post-production and motion graphics.</p>
                        <a href="/services/video-editing" class="btn btn-primary" style="margin-top: 20px; display: inline-block;">Details</a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Blog Section -->
    <section class="section-padding" id="blogs">
        <h2 class="reveal" style="font-size: 3.5rem; text-align: center;">Latest From <span class="gradient-text">Our Blog</span></h2>
        
        <div class="blog-grid">
            <div class="blog-card reveal">
                <div class="blog-img" style="background-image: url('https://images.unsplash.com/photo-1498050108023-c5249f4df085?q=80&w=2072&auto=format&fit=crop');"></div>
                <div class="blog-content">
                    <span style="background: var(--primary-orange); color: white; padding: 4px 12px; border-radius: 20px; font-size: 0.7rem;">TECH</span>
                    <h3 style="margin: 15px 0; color: white;">The Future of React 19</h3>
                    <p style="color: var(--text-dim); font-size: 0.9rem;">Exploring the upcoming changes in the world's most popular UI library.</p>
                    <a href="#" style="display:block; margin-top: 15px; color: var(--primary-orange); text-decoration: none; font-weight: bold;">Read More →</a>
                </div>
            </div>
            <div class="blog-card reveal">
                <div class="blog-img" style="background-image: url('https://images.unsplash.com/photo-1555066931-4365d14bab8c?q=80&w=2070&auto=format&fit=crop');"></div>
                <div class="blog-content">
                    <span style="background: var(--primary-blue); color: white; padding: 4px 12px; border-radius: 20px; font-size: 0.7rem;">EDUCATION</span>
                    <h3 style="margin: 15px 0; color: white;">Why Every Teen Should Code</h3>
                    <p style="color: var(--text-dim); font-size: 0.9rem;">Coding is more than just a job skill—it's a new way of thinking.</p>
                    <a href="#" style="display:block; margin-top: 15px; color: var(--primary-orange); text-decoration: none; font-weight: bold;">Read More →</a>
                </div>
            </div>
            <div class="blog-card reveal">
                <div class="blog-img" style="background-image: url('https://images.unsplash.com/photo-1581091226825-a6a2a5aee158?q=80&w=2070&auto=format&fit=crop');"></div>
                <div class="blog-content">
                    <span style="background: var(--primary-orange); color: white; padding: 4px 12px; border-radius: 20px; font-size: 0.7rem;">AI</span>
                    <h3 style="margin: 15px 0; color: white;">AI in Creative Design</h3>
                    <p style="color: var(--text-dim); font-size: 0.9rem;">How generative AI is helping designers work 10x faster.</p>
                    <a href="#" style="display:block; margin-top: 15px; color: var(--primary-orange); text-decoration: none; font-weight: bold;">Read More →</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-grid">
            <div class="footer-col">
                <a href="/" class="logo" style="height: 60px; margin-bottom: 20px;">
                    <img src="/images/logo-binary.png" alt="Binary Hub Logo">
                </a>
                <p style="margin-top: 20px; color: #aaa;">Empowering the next generation through digital excellence and innovative learning.</p>
                <div class="social-icons">
                    <a href="#"><i class="fab fa-facebook-f"></i></a>
                    <a href="#"><i class="fab fa-instagram"></i></a>
                    <a href="#"><i class="fab fa-linkedin-in"></i></a>
                    <a href="#"><i class="fab fa-youtube"></i></a>
                </div>
            </div>
            <div class="footer-col">
                <h4>Quick Links</h4>
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/our-mission">Our Mission</a></li>
                    <li><a href="/impact">Our Impact</a></li>
                    <li><a href="/branches">Our Branches</a></li>
                    <li><a href="/events">Events</a></li>
                    <li><a href="/visits">Visits</a></li>
                    <li><a href="/courses">Courses</a></li>
                    <li><a href="/team">Our Team</a></li>
                    <li><a href="/services">Services</a></li>
                    <li><a href="/internships">Internships</a></li>
                </ul>
            </div>
            <div class="footer-col">
                <h4>Courses</h4>
                <ul>
                    <li><a href="#">Web Development</a></li>
                    <li><a href="#">Graphic Design</a></li>
                    <li><a href="#">Data Science</a></li>
                    <li><a href="#">Cyber Security</a></li>
                </ul>
            </div>
            <div class="footer-col">
                <h4>Contact Us</h4>
                <ul style="color: #aaa;">
                    <li><i class="fas fa-map-marker-alt"></i> 123 Tech Avenue, Digital City</li>
                    <li><i class="fas fa-phone"></i> +1 (234) 567 890</li>
                    <li><i class="fas fa-envelope"></i> hello@binaryhub.tech</li>
                </ul>
            </div>
        </div>
        <div style="border-top: 1px solid rgba(255,255,255,0.1); padding-top: 30px; text-align: center; color: #777; font-size: 0.8rem;">
            &copy; 2025 Binary Hub. All Rights Reserved. Designed with ❤️ by Binary Hub Team.
        </div>
    </footer>

    <a href="#" id="backToTop"><i class="fas fa-arrow-up"></i></a>

    <script>
        // Loader
        window.addEventListener('load', () => {
            setTimeout(() => {
                document.getElementById('loader').style.opacity = '0';
                setTimeout(() => {
                    document.getElementById('loader').style.display = 'none';
                }, 800);
            }, 1500); // Reduced delay for better UX
        });

        // Custom Cursor
        const glow = document.getElementById('cursor-glow');
        const dot = document.getElementById('cursor-dot');
        
        document.addEventListener('mousemove', (e) => {
            const x = e.clientX;
            const y = e.clientY;
            
            // Glow follows with a slight delay if desired, or instantly
            glow.style.left = x + 'px';
            glow.style.top = y + 'px';
            
            // Dot follows instantly
            dot.style.left = x + 'px';
            dot.style.top = y + 'px';
        });

        document.querySelectorAll('a, button, .dot, .orbit-item').forEach(el => {
            el.addEventListener('mouseenter', () => {
                glow.style.width = '120px';
                glow.style.height = '120px';
                glow.style.background = 'radial-gradient(circle, rgba(238,112,43,0.3) 0%, transparent 70%)';
                dot.style.transform = 'translate(-50%, -50%) scale(2.5)';
                dot.style.background = 'var(--primary-blue)';
            });
            el.addEventListener('mouseleave', () => {
                glow.style.width = '300px';
                glow.style.height = '300px';
                glow.style.background = 'radial-gradient(circle, rgba(238,112,43,0.15) 0%, rgba(0,86,150,0.05) 50%, transparent 70%)';
                dot.style.transform = 'translate(-50%, -50%) scale(1)';
                dot.style.background = 'var(--primary-orange)';
            });
        });

        // Sticky Navbar & Scroll Progress
        const navbar = document.getElementById('navbar');
        const progressBar = document.getElementById('progressBar');
        const btt = document.getElementById('backToTop');

        window.addEventListener('scroll', () => {
            const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
            const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            const scrolled = (winScroll / height) * 100;
            progressBar.style.width = scrolled + "%";

            if (winScroll > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }

            if (winScroll > 300) {
                btt.classList.add('show');
            } else {
                btt.classList.remove('show');
            }
        });

        // Hero Slider Logic
        let currentSlide = 0;
        const slides = document.querySelectorAll('.slide');
        const dots = document.querySelectorAll('.dot');

        function setSlide(n) {
            slides[currentSlide].classList.remove('active');
            dots[currentSlide].classList.remove('active');
            dots[currentSlide].style.background = 'white';
            
            currentSlide = n;
            
            slides[currentSlide].classList.add('active');
            dots[currentSlide].classList.add('active');
            dots[currentSlide].style.background = 'var(--primary-orange)';
        }

        setInterval(() => {
            let next = (currentSlide + 1) % slides.length;
            setSlide(next);
        }, 5000);

        // Orbital Layout Positioning with Rotation
        let orbitRotation = 0;
        const orbitLinesContainer = document.getElementById('orbit-lines');
        
        // Initialize SVG lines
        function initOrbitLines() {
            const orbitItems = document.querySelectorAll('.orbit-item');
            orbitLinesContainer.innerHTML = '';
            orbitItems.forEach(() => {
                const line = document.createElementNS("http://www.w3.org/2000/svg", "line");
                line.setAttribute("stroke", "rgba(238, 112, 43, 0.3)");
                line.setAttribute("stroke-width", "1");
                line.setAttribute("stroke-dasharray", "4,4");
                orbitLinesContainer.appendChild(line);
            });
        }

        function positionOrbits() {
            const orbitItems = document.querySelectorAll('.orbit-item');
            const lines = orbitLinesContainer.querySelectorAll('line');
            const width = window.innerWidth;
            const centerX = orbitLinesContainer.clientWidth / 2;
            const centerY = orbitLinesContainer.clientHeight / 2;
            let radius = 0;

            if (width > 768) {
                radius = width > 1024 ? 350 : 280;
                orbitItems.forEach((item, i) => {
                    const angle = (i * 60 + orbitRotation) * (Math.PI / 180);
                    const x = Math.cos(angle) * radius;
                    const y = Math.sin(angle) * radius;
                    
                    // Position item
                    item.style.transform = `translate(calc(-50% + ${x}px), calc(-50% + ${y}px))`;
                    item.style.display = 'flex';

                    // Position line
                    if (lines[i]) {
                        lines[i].setAttribute("x1", centerX);
                        lines[i].setAttribute("y1", centerY);
                        lines[i].setAttribute("x2", centerX + x);
                        lines[i].setAttribute("y2", centerY + y);
                        lines[i].style.opacity = "1";
                    }
                });
            } else {
                orbitItems.forEach((item, i) => {
                    item.style.transform = 'none';
                    if (lines[i]) lines[i].style.opacity = "0";
                });
            }
        }

        initOrbitLines();
        
        function animateOrbit() {
            orbitRotation += 0.15;
            positionOrbits();
            requestAnimationFrame(animateOrbit);
        }

        animateOrbit();
        window.addEventListener('resize', () => {
            initOrbitLines();
            positionOrbits();
        });

        // Mobile Menu Toggle
        const mobileToggle = document.getElementById('mobileToggle');
        const navLinks = document.querySelector('.nav-links');

        mobileToggle.addEventListener('click', (e) => {
            e.stopPropagation();
            navLinks.classList.toggle('active');
            const icon = mobileToggle.querySelector('i');
            if (navLinks.classList.contains('active')) {
                icon.classList.replace('fa-bars', 'fa-times');
            } else {
                icon.classList.replace('fa-times', 'fa-bars');
            }
        });

        // Dropdown toggle on mobile
        document.querySelectorAll('.dropdown > a').forEach(dropdownToggle => {
            dropdownToggle.addEventListener('click', (e) => {
                if (window.innerWidth <= 768) {
                    e.preventDefault();
                    e.stopPropagation();
                    const parent = dropdownToggle.parentElement;
                    parent.classList.toggle('open');
                }
            });
        });

        // Close menu on link click
        document.querySelectorAll('.nav-links a:not(.dropdown > a)').forEach(link => {
            link.addEventListener('click', () => {
                navLinks.classList.remove('active');
                mobileToggle.querySelector('i').classList.replace('fa-times', 'fa-bars');
                document.querySelectorAll('.dropdown').forEach(d => d.classList.remove('open'));
            });
        });

        // Close on outside click
        document.addEventListener('click', (e) => {
            if (!navLinks.contains(e.target) && !mobileToggle.contains(e.target)) {
                navLinks.classList.remove('active');
                mobileToggle.querySelector('i').classList.replace('fa-times', 'fa-bars');
            }
        });

        // Intersection Observer for Reveal
        const observerOptions = { threshold: 0.15 };
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('active');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.reveal').forEach(el => observer.observe(el));


    </script>
</body>
</html>